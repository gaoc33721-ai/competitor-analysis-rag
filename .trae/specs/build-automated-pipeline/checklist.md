- [x] 爬虫模块能够成功获取符合规范的商品结构化数据
- [x] AI 处理模块能够稳定调用 MiniMax 接口，即使部分请求失败也能重试或跳过，不中断整个流程
- [x] 最终产出的数据包含所有必需字段（特别是 `ai_tags` 和 `ai_analysis`），且能成功写入存储
- [x] 定时调度脚本能够正常运行，并能在设定的时间点触发 Pipeline
- [x] Pipeline 运行过程中有清晰的控制台或文件日志输出